# Repository instructions

- `rules/` contains path-scoped engineering contracts.
- `skills/` contains automatically selected workflows.
- `CLAUDE.md` is the always-on repository contract.
- `AGENTS.md` is the discovery shim for compatible clients.

The same public boundary applies regardless of which client performs the work.
